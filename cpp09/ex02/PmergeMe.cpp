#include "PmergeMe.hpp"


std::vector<int>	PmergeMe::JacobsthalSequence(int	size)
{
	std::vector<int> jacobNumbers;
	int				 prevGroupIndex = 1;
	int				 currentGroupIndex = 3;

	jacobNumbers.push_back(2);
	jacobNumbers.push_back(1);

	while (prevGroupIndex < size)
	{
		//a finir - ensuite retourner dans le .tpp pour finir la fonciton fordjonson et binarysearch et ca devrait etre fini.
	}

}
